발표 희망합니다.

## 운전자 졸음 감지 시스템 개발 프로젝트 최종 보고서

### 1. 프로젝트 소개

본 프로젝트는 딥러닝 기술을 활용하여 운전자의 하품 여부 및 눈 감김 상태를 실시간으로 감지하고, 이를 통해 졸음 가능성을 판단하는 저비용 개인 맞춤형 시스템을 구축하는 것을 목표로 합니다. 기존 시스템의 고비용 생체 센서 의존성 및 개인차 미반영 문제를 해결합니다. 운전자 졸음은 심각한 교통사고의 주요 원인 중 하나로, 운전자의 안전뿐만 아니라 타인의 안전에도 지대한 영향을 미칩니다. 기존의 졸음 감지 시스템은 고가의 생체 센서(예: 뇌파, 심박수)에 의존하거나, 모든 운전자에게 일률적인 기준을 적용하여 개인별 운전 습관이나 생체적 특성을 반영하지 못한다는 한계가 있었습니다. 본 프로젝트는 이러한 문제를 해결하고자, 운전자 얼굴 이미지라는 저비용의 시각적 데이터를 활용합니다. 눈 감김(Eye Closure)과 하품(Yawning)은 졸음의 가장 명확하고 보편적인 징후이며, 이를 딥러닝 모델로 실시간 분석함으로써 경제적이면서도 효과적인 졸음 감지 솔루션을 제공하는 것을 목표로 합니다.


### 2. 주제 관련 배경 및 용어 정리

* **Dlib:** 얼굴 감지(Face Detection) 및 얼굴 특징점(Facial Landmarks) 검출에 널리 사용되는 C++ 기반의 머신러닝 라이브러리입니다. 본 프로젝트에서는 운전자의 얼굴을 찾고 눈과 입의 위치를 정확히 파악하는 데 사용되었습니다.
* **얼굴 특징점(facial landmarks):** 눈꼬리, 눈썹, 콧망울, 입꼬리 등 얼굴의 특정 부위를 나타내는 좌표점들입니다. Dlib는 68개의 특징점을 일반적으로 사용하며, 이를 통해 눈이 감겼는지, 입이 벌어졌는지 등을 수치적으로 분석할 수 있습니다.
* **class_weight:** 모델 학습 시 데이터셋에 **클래스 불균형(Class Imbalance)**이 있을 때 사용하는 파라미터입니다. 샘플 수가 적은 소수 클래스의 손실에 더 큰 가중치를 부여하여, 모델이 소수 클래스의 패턴을 더 중요하게 학습하도록 유도함으로써 불균형 문제를 해결하고 성능을 개선합니다.

### 3. 데이터셋 소개

졸음 감지 시스템 학습을 위해 다음과 같은 공개 데이터셋을 활용할 수 있습니다:

* **눈 상태 분류 (Open/Closed Eye):**
    * **Drowsiness Detection Dataset (by prasadvpatil):** 이 데이터셋은 Closed_Eyes와 Open_Eyes 폴더로 구성되어 운전자의 눈 감김 및 떠짐 상태를 분류하는 모델 학습에 사용되었습니다. 이 데이터셋에는 별도의 테스트 폴더가 없어, 훈련 데이터 내에서 80%를 훈련, 20%를 검증 데이터로 분할하여 사용했습니다.(https://www.kaggle.com/datasets/prasadvpatil/mrl-dataset)
 
* **하품 감지 (Yawn/No Yawn):**
    * **Yawn Dataset (by davidvazquezcic):**  이 데이터셋은 yawn과 no yawn 폴더로 구성되어 운전자의 하품 여부를 분류하는 모델 학습에 활용되었습니다. 이 데이터셋 역시 훈련 데이터 내에서 80%를 훈련, 20%를 검증 데이터로 분할하여 사용했습니다.(https://www.kaggle.com/datasets/davidvazquezcic/yawn-dataset)
  
* **영상 데이터:**
   * ai 영상 제작 툴인 Hailuo AI에서 normal.mp4(평범하게 운전하는 모습), sleepy.mp4(졸면서 운전하는 모습) 각각 5초 분량의 영상 데이터를 생성했습니다.(Driver Activity Video(https://www.kaggle.com/datasets/kunalrawat/test-video) 데이터를 사용하고자 했으나 모델이 알아듣질 못해서 ai툴을 활용했습니다..)

### 4. 전처리 과정

**이미지 데이터셋을 딥러닝 모델에 적합하도록 전처리하고, 모델의 일반화 성능을 높이기 위해 데이터 증강(Data Augmentation) 기법을 적극적으로 사용했습니다.**

1.  **눈 감지:** ImageDataGenerator를 이용해 픽셀 값을 0~1로 정규화
2.  **데이터 증강(Data Augmentation):** 데이터의 다양성을 높이고 모델의 일반화 성능을 향상시키기 위해 훈련 이미지에 대해 회전, 확대/축소, 좌우 반전 등 기하학적 변환을 적용합니다. 눈 감지, 하품 감지를 목적으로 진행했습니다.
3.  **정규화 및 크기 조정:** 딥러닝 모델의 입력으로 사용하기 위해 이미지 픽셀 값을 정규화하고, 모델이 요구하는 입력 크기(128x128 크)로 조정합니다.

### 5. 모델 구조

- MobileNetV2 기반의 전이 학습(Transfer Learning) 모델을 구축했습니다.

* **기본 모델 구조 (초기 CNN 시도):** 여러 개의 Conv2D (합성곱), MaxPooling2D (최대 풀링) 블록을 쌓아 이미지 특징을 추출했습니다. Flatten 층으로 특징 맵을 평탄화하고, Dense (완전 연결) 층을 통해 분류를 수행했습니다. Dropout 층을 사용하여 과적합을 방지하려 했습니다. 하지만 과적합 문제가 심해개 발생했습니다.

* **눈 감지 모델 (MobileNetV2 전이 학습):**:
*  베이스 모델: applications.MobileNetV2(include_top=False, weights='imagenet')를 사용하여 ImageNet으로 사전 학습된 MobileNetV2의 특징 추출 부분을 가져왔습니다.
   동결(Freezing): base_model.trainable = False로 베이스 모델의 모든 층을 동결하여, 이미 학습된 강력한 지식을 보호하고 작은 데이터셋에서의 과적합을 방지했습니다.
   새로운 분류 헤드: 베이스 모델 위에 GlobalAveragePooling2D, Dense(128, activation='relu'), BatchNormalization, Dropout(0.5), 최종 Dense(1, activation='sigmoid') 층을 추가하여 눈 감김/떠짐을 분류하도록 했습니다.
   컴파일: optimizer='adam', loss='binary_crossentropy', metrics=['accuracy'].
   콜백: EarlyStopping(patience=10)과 ModelCheckpoint를 사용하여 효율적인 학습과 최적 모델 저장을 수행했습니다.


* **하품 감지 모델(MobileNetV2 전이 학습 + 미세 조정) :**
* 눈 감지 모델과 동일한 MobileNetV2 기반의 전이 학습 모델 구조를 사용했습니다.
   모델 복잡도 조정: 과적합 완화를 위해 Dense 층의 뉴런 수를 128개에서 64개로 감소시키고, Dropout 비율을 0.6으로 증가시켰습니다.
   L2 정규화 추가: layers.Dense 층에 kernel_regularizer=tf.keras.regularizers.l2(0.001)을 추가하여 가중치 크기에 페널티를 부여함으로써 과적합을 추가적으로 억제했습니다.
   데이터 불균형 처리: fit() 함수에 class_weight 딕셔너리({0: 'yawn' 가중치, 1: 'no yawn' 가중치})를 전달하여, 'yawn'과 'no yawn' 클래스 이미지 개수의 불균형을 보정하고 소수 클래스에 대한 학습을 강화했습니다.
   미세 조정(Fine-tuning):
   1단계: 베이스 모델 동결 상태에서 새로운 분류 헤드만 학습시켰습니다.7
   2단계: 1단계 학습 후, yawn_base_model.trainable = True로 베이스 모델의 모든 층을 동결 해제했습니다. 이후 **매우 낮은 학습률(tf.keras.optimizers.Adam(learning_rate=1e-5))**로 모델 전체를 다시 컴파일하고 추가 학습을 진행하여, 모델이 하품 데이터셋의 미묘한 특징까지 학습하도록 정교화했습니다.


### 6. 레퍼런스 개선점 

기존 졸음 감지 시스템의 **개인차 미반영** 문제를 해결하고 개인 맞춤형 시스템을 구현하기 위한 핵심 전략은 다음과 같습니다.

* **전이 학습 (Transfer Learning) 및 온라인 미세 조정 (Online Personalized Fine-Tuning, OPFT):**
    * 대규모 공개 데이터셋으로 사전 학습된 모델(예: MobileNet)을 기반으로 합니다.
    * 실제 운전 환경에서 운전자 개인의 데이터(초기 캘리브레이션 또는 지속적인 모니터링 중 수집된 데이터)를 사용하여 모델의 특정 레이어를 미세 조정(fine-tuning)합니다. 이를 통해 모델이 특정 운전자의 미묘한 졸음 징후에 더 민감하게 반응하도록 맞춤화할 수 있습니다.
    * 이는 모델이 새로운 운전자에게 빠르게 적응하고, 시간이 지남에 따라 운전자의 행동 변화에 대응할 수 있도록 합니다.

### 7. 프로젝트 결과 (성능 평가 및 지표)

프로젝트 결과는 정확도, 정밀도, 재현율, F1-score 등을 이용해 모델의 성능과 실시간 처리 능력을 평가합니다.

   * **눈 감지 모델:** MobileNetV2 전이 학습 도입 후 **훈련 정확도 98~99%, 검증 정확도 95~99%**의 매우 높은 성능을 달성했습니다. 훈련 손실과 검증 손실의 격차가 현저히 줄어들어 과적합 문제가 효과적으로 해결되었음을 확인했습니다. 모델은 눈 감김/떠짐 상태를 매우 안정적으로 분류할 수 있게 되었습니다.
   * **하품 감지 모델:** class_weight 적용 및 MobileNetV2 미세 조정을 통해 **훈련 정확도 98~99%, 검증 정확도 95~99%**에 이르는 우수한 성능을 달성했습니다. 이전 학습에서 보였던 검증 성능의 불안정성과 과적합 문제가 크게 개선되어, 하품 여부도 안정적으로 감지할 수 있게 되었습니다.
   * **통합 시스템 동작:** Dlib를 이용한 얼굴 및 특징점 감지, 그리고 학습된 두 모델을 활용한 비디오 프레임 처리를 구현했습니다. ai 영상 제작 툴을 이용해 만든 영상인 normal.mp4와 sleepy.mp4 테스트 결과, 비디오 내용에 따라 졸음_감지 또는 정상 상태가 출력되는 것을 확인했습니다. 하지만 정확히 예측하는데는 실패했습니다. 


### 8. 추후 발전 방향

* **다중 모달 데이터 융합:**
    * 카메라 기반 시각 정보(눈, 입, 고개 움직임) 외에 운전 패턴(차선 이탈, 흔들림), 음성(하품 소리), 심박수 등 추가적인 생체 신호(저비용 센서 활용)를 통합하여 졸음 감지의 정확도와 신뢰성을 높입니다.
* **강건성 및 일반화 향상:**
    * 다양한 조명 조건(야간, 역광 등), 안경 착용, 마스크 착용 등 현실적인 운전 환경에서의 모델 강건성을 확보하기 위한 데이터셋 확장 및 모델 최적화가 필요합니다.
    * 다양한 인종, 연령, 성별의 운전자 데이터를 추가하여 모델의 일반화 성능을 개선합니다.
* **경고 및 피드백 시스템 고도화:**
    * 단순 안내 외에 시각적 알림, 진동 시트, 커피점 추천 등 운전자의 졸음을 효과적으로 해소하고 안전운전을 유도할 수 있는 다양한 형태의 경고 및 피드백 메커니즘을 개발합니다.
